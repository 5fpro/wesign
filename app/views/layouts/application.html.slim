doctype 5
html lang="zh-TW"
  head
    = render "layouts/html-head"
  body
    = render "partials/header"
    - flash.each do |k, message|
      div class="alert-#{k}"
        = message
    = yield
    = render "partials/footer"
    / = render "partials/modal-group"
    = render "layouts/html-foot"